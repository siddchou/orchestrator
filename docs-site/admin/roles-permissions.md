# Roles & Permissions

This page provides the complete RBAC (Role-Based Access Control) matrix for Novakai Orchestrator, detailing which endpoints each role can access.

## Role Definitions

Descriptions of the built-in roles: `ADMIN`, `OPERATOR`, and `VIEWER`, including what each role is intended to do.

## Permission Matrix

A comprehensive table mapping every API endpoint to the roles that have access, with HTTP method restrictions noted per row.

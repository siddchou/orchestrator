# Credentials

This page covers the encrypted credential store and SSH key pair generation for secure step execution.

## Credential Management

How to create, view (masked), update, and delete credentials stored in the system, including supported credential types.

## Encryption

Details on how credentials are encrypted at rest, key management practices, and what happens during key rotation.

## SSH Key Generation

Step-by-step instructions for generating SSH key pairs within the platform for use by `SFTP` step types or other remote execution steps.

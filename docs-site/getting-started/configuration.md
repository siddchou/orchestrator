# Configuration

This page documents every environment variable, Spring profile, and `application.yml` property available in Novakai Orchestrator.

## Environment Variables

A reference table of all environment variables that control server behavior, database connections, authentication settings, and feature flags will be listed here.

## Spring Profiles

Descriptions of each Spring Boot profile (e.g., `dev`, `test`, `prod`) and the configuration defaults they activate.

## application.yml Reference

A complete property reference for `application.yml`, organized by namespace, with default values and descriptions for each setting.
